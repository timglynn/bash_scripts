MediaWiki backup and wrapper
============================

* Note - both scripts use secrets sourced in the `ENV`

* Example -> `mediawiki_secrets.txt`:
    
    ```
    REMOTE_MEDIA_WIKI_USER=foo
    export REMOTE_MEDIA_WIKI_USER
    ```
